# PiAbyss 需求文档

## 一、需求清单

> 序号后紧跟 `***` 的为重要且紧急项；按字面实现难度分类（纯样式 / 文案 / 简单删除 / 小交互调整归「轻」，新功能或较大改造归「重」，疑问或需先调研归「待研究」）。
>
> 已完全实现或完成的功能（含单界面改造、包管理、技能页、附件体系、视觉模型回退配置等）已从本清单移除，此处仅保留未完成项。

### 已完成（备忘录 v1 · 纯本地）

- **备忘录中心**：侧栏「周期计划」下方新增入口，主对话区内全新页面（遵守单界面原则，无覆盖层）；
  - 记录模型：`type`（备忘/想法/任务）+ 标题 + Markdown 正文 + 图片 + 标签 + 工作区关联标签（可选，仅作筛选/提示，不与工作区强绑定）；
  - 状态归类：进行中 / 已完成 / 已归档三个页签，支持搜索与标签、工作区筛选；
  - 「用 Agent 处理」：把记录以 `<piabyss-memo>` 引用块注入当前会话草稿并切换到对话页，agent 完成后调用内置 `piabyss_memo` 工具把记录标记为完成（Host 内置 customTool，带激活扩展，同名磁盘包无法遮蔽）；
  - 存储：`<agentDir>/piabyss/memo/notes.json` + `images/<noteId>/`，磁盘权威、原子写入、多实例安全；协议层 `memo.*`（list/create/update/delete/readImage）带参数与结果双重校验；
  - 后续规划（v2）：~~Cloudflare R2 同步 + 恢复 + 多设备~~（已完成，见「备忘录 v2」）、Cloudflare Worker + D1 双向同步增强（outbox 队列）、agent 工具只读上下文接入。

### 已完成（备忘录 v1.1 · 结果总结 + 会话关联）

- **Agent 结果总结**：`piabyss_memo` 工具 `complete` 动作必须提交 `result` 总结（覆盖式写入 `MemoNote.result`）；手动标记完成不产生总结；重新处理会覆盖旧总结；
- **会话关联**：工具执行时自动捕获提交总结所在的会话（id / 路径 / 标题 / cwd），无需前端上报；
- **前端**：已完成且有总结的记录，详情按钮切换为「Agent完成总结」弹窗（查看总结 + 来源会话 + 「继续讨论」：原会话存在则跨工作区跳转，否则新建会话并注入引用块 + 最新总结 + 指令草稿）。

### 已完成（备忘录 v2 · Cloudflare R2 云同步）

- **双向同步**：`memo.syncNow` = 拉取云端 notes.json → 与本地逐条合并（按 updatedAt 最后写入者胜）→ 回写本地并补齐缺失图片 → 上传合并结果；删除通过墓碑（`deletedAt`，软删除保留 30 天）传播到其他设备，删除后的编辑（updatedAt 更新）可复活记录；30 天过期墓碑由同步时物理清理（含云端图片对象 DELETE）；
- **恢复**：`memo.restoreFromCloud` 以云端快照为准覆盖本地（破坏性，前端两步确认）；
- **配置**：`memo.getSyncConfig / memo.setSyncConfig / memo.testSync`，持久化在 `<agentDir>/piabyss/memo/sync-config.json`（含 lastSync 状态回写）；密钥仅存本机；
- **R2 客户端**：`r2-client.ts` 自实现 AWS SigV4（region=auto、service=s3、path-style），不引入 AWS SDK；
- **自动同步**：`autoSync` 开启时，备忘录每次变更（create/update/delete）后防抖 5s 后台上传；
- **前端**：顶栏（AppTopBar 右侧，仅备忘录页显示）两个入口——「立即同步」（一键双向同步，未配置时提示）和「云同步设置」（带状态点：灰=未配置/未同步、绿=最近同步成功、红=最近同步失败，30s 轮询刷新）→ 配置弹窗（Account ID / Access Key / Secret / 桶名 / autoSync + 测试连接 / 立即同步 / 保存 + 最近同步状态 + 带色调的操作反馈条）；同步/保存成功后广播事件刷新备忘录列表；不再区分「恢复」与「同步」——合并式同步天然覆盖恢复语义。

### 难度：轻
1. 文本的行间距需要增加一点，不然看着太乱了；（暂不调整：暂时还能接受）

### 难度：重
2. 增加文件索引功能（不急）；
3. 界面缩放；
6. 插件按工作区添加白名单：插件仍做**全局安装 / 全局配置**（沿用现有全局插件机制），但不同工作区（项目目录）能够使用的全局插件可以不一样——每个插件增加一个「允许使用的工作区」白名单配置（按项目路径匹配，如 `D:/work/project-a`；支持通配或目录前缀）。白名单为空 = 所有工作区默认可用（保持现有行为）；白名单非空 = 仅列出的工作区可加载该插件，其余工作区自动禁用。可选反向视角：工作区侧配置允许使用的插件列表。
   - 界面约束：遵守「单界面」原则，白名单配置在主对话区内承载，不新增覆盖层；
   - 兼容性：白名单为空时行为与现状完全一致，不影响已安装插件的用户；
   - 加载时机：工作区确定（会话启动 / 切换 / 重载）时按 cwd 匹配白名单决定插件是否注册，参考现有扩展的 cwd 判断能力实现。

### 待研究
4. ***对话区显示混乱，不够清晰；（描述模糊，需先定位是哪里混乱）
5. agent 性格；

---

## 二、设计原则：单界面、不再使用二级覆盖界面

应用整体保持**单主界面**结构，不再新增任何会盖住主界面的二级覆盖层（Overlay / 弹窗 / 全屏遮罩 / 独立设置页等）。

- **已完成**：设置与包管理此前以 `SettingsOverlay` 整层覆盖主界面，现已改造为在主对话区内渲染（设置 / 包管理各自占用 `<main>` 显示区域，侧栏、顶栏、右 Dock 保持可见可交互），Overlay 覆盖层已删除。
- **未来的约束**：后续新增任何配置/管理类功能（例如扩展面板等），也一律在主界面内承载——通过切换主对话区的显示内容、或替换 `<main>` 里渲染的组件来实现，而**不再叠一层覆盖在主界面上**。侧栏、顶部对话头、右 Dock 等主界面元素在所有视图下都应保持可见可交互。
- **例外**：必要的轻量临时遮挡（确认对话框、菜单、tooltip、拖放提示）不在此约束范围内，它们是临时浮层、不是常驻视图。

---

## 三、辅助模型配置

### 当前情况
- pi 核心没有统一的辅助模型配置项；自动压缩、分支摘要默认使用当前会话模型。
- PiAbyss 已支持自动优化会话标题、生成 Git 提交说明，但目前都使用当前会话的主模型。
- `pi-subagents` 已支持为不同子代理单独指定模型，例如为 `scout`、`reviewer`、`worker` 配置不同模型。
- 视觉模型回退配置**已完成**：插件库配置项支持 `optionsSource: "pi:vision-models"`，可将「默认视觉模型」渲染为由运行时模型列表动态生成的"供应商名 · 模型名"单选下拉，供 pi-vision 等插件在 `PI_VISION_MODEL` 环境变量中使用。

### 后续需求
增加 PiAbyss 的辅助模型配置，可分别指定：
- 会话标题模型；
- Git 提交说明模型；
- 自动压缩/摘要模型。

（视觉模型已可通过插件库配置，无需再单独建模。）
